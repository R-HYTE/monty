#include "monty.h"

/**
 * _stack - Flag allocation in case of a stack
 * @stack: pointer to the pointer to the list
 * @line_number: current line number in the file being read
 * Return: Void
 */
void _stack(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
	pub.ads_flag = 0;
}
