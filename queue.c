#include "monty.h"

/**
 * _queue - Flag allocation for queues case
 * @stack: pointer to the pointer to the head of the stack
 * @line_number: current line number in the file being read
 * Return: Void
 */
void _queue(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
	pub.ads_flag = 1;
}
